# mamba env create -f gff3toolkit.yml
name: gff3toolkit
channels:
 - defaults
 - bioconda
dependencies:
 - python=3.11
 - gffread=0.12.7
 - pip
 - pip:
  # works for regular pip packages
   - gff3tool
